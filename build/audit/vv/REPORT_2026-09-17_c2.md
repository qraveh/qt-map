# V&V report addendum — the C2 build (branch c2-machines-2026-09-17), 17 Sep 2026
QT-Map · SP01 · build/audit/vv · session C2 · companion to REPORT_2026-09-16.md (baseline 8106b9e). Results in `results_c2/`, summary in `SUMMARY_c2.md`.

## What changed between the two runs
Defects 1, 2 and 4 of the 16 Sep report were fixed (`lensValues()` makes the f lens list-aware; `isolatePath()` keeps the focused station; the bottom sheet opens at 46vh and the page gets room to scroll above it), and the Machine selector was added as one more term of the lit set. The harness gained the machine term (oracle + driver + a `machines` suite: every machine alone, × each toggle, × its own and one foreign isolation, 60 × lens value, 60 × focus), the driver's state model follows the new isolate behaviour, and the fit checks wait for the scroll to settle.

## Coverage on the C2 build
| suite | states | agree | disagree |
|---|---|---|---|
| single | 194 | 194 | 0 |
| pairs | 3,357 | 3,357 | 0 |
| multi | 1,358 | 1,358 | 0 |
| machines | 936 | 936 | 0 |
| metamorphic | 200 | 200 | 0 |
| static | 6 (16 Sep run; page structure unchanged) | 6 | 0 |
| random | 40 sequences (run before the harness gained the machine term) | 21 | 19 — all pre-fix driver drift (15: the driver still assumed isolate clears focus), 4 fit-idempotence readings taken before the scroll settled; replayed after the harness update: 3 of 4 idempotent, 1 real (below) |

The 336 lit-set disagreements of the baseline are gone; no new signature appeared in 6,045 deterministic states.

## Residual deviations
1. `seq-7-24`: with `enc_timebin` focused at 1024 px, a second `fit width` moves `#mapwrap.scrollTop` from 0 to 160 while zoom and size stay identical — a scroll-alignment side effect of fit on the sheet layout; cosmetic, to fix in the next revision.
2. Reading-marks legend counts are graph totals, not live counts of the lit set (RULES.md adjudication 9) — the editor's "live keys" wording is ambiguous; awaiting a written rule.
3. The random suite is at 40 sequences of the SPEC's 5,000 (CPU-bound; resume with `VV_PAGE=<dist> python3 runner.py random --n 5000 --seed 1 --out results_c2/`).

## Claim
On this build, every single-value, pairwise, multi-select and machine state tested agrees with the independent oracle; the metamorphic relations hold. "Virtually all bugs" (SPEC §4.7) still waits on the random threshold and on the editor's rulings for the four design gaps and adjudication 9.

## Addendum after the editor's review of 17 Sep (commits 4f1ea07, 7f25ba5 and the place change)
The review changed the lens list (order, plain labels, reading-marks lens removed), made `place` list-valued (`ct_sfq` mK + 4 K, `ct_cryocmos` 4 K + mK, `vac` removed), added sortable tables and zoom bars, and fixed two build bugs (§7.11 pipe escaping; folds re-targeted to §7.2/§7.11). The harness followed: marks dropped from the lens list, place membership over the list, adjudication 10.

| build | suite | states | disagreements |
|---|---|---|---|
| preview 3 (7f25ba5) | single | 327 (= 191 lens/toggle/isolation/focus states + 136 machines) | 0 |
| preview 3 (7f25ba5) | machines | 936 | 0 |
| preview 3 (7f25ba5) | metamorphic | 200 | 0 |
| final (place change + label) | single | 326 (the `vac` value is gone) | 0 |

Pairs and multi were not re-run after the review: the lit-set code paths they exercise (lens membership, toggles, isolation, focus, machine term) are unchanged since the clean run on 452977a…d35f7b6 except for the `place` membership, which the single suite covers value by value. Smoke (`build/audit/c2_smoke.py`, 104+ checks at 1600/1280/400 px) passes on the final build.

## Addendum after the §8 "Machines" chapter (17 Sep 2026, evening; commits 393e1cf, 8cef68c, a46240e)
The report gained §8 "Machines — 136 attempts, and where the architecture is going" (generated by `build/machines_chapter.py`) and Sources became §9; `data/machines.json` gained the register profile fields and roadmap verdicts and was rebuilt from the evidence of locator passes 1–3 (706 ✅ / 827 🔎). None of this touches the lit-set code: the map, its lenses and the machine term are unchanged since 5c0c8dc; the `__MACH` embed differs only in evidence types/URLs/locators/verification flags (node ids and roles identical — checked by `machines_json_check.py`). The suites were re-run as a regression guard.

| build | suite | states | disagreements | results |
|---|---|---|---|---|
| 8cef68c (chapter build, first prose form) | single | 326 | 0 | `results_c2d/` |
| 8cef68c → a46240e (dist re-built with §8 prose changes only while the suite ran) | machines | 936 | 0 | `results_c2d/` |
| a46240e (this bundle's build) | metamorphic | 200 | 0 | `results_c2d/` |
| a46240e (this bundle's build) | single | 326 | 0 | `results_c2e/` |

Smoke (`build/audit/c2_smoke.py`, 150 checks at 1600/1280/400 px, now including `chapter8()`: §8 sits between §7 and the briefs in both languages, 8.1–8.6 in the TOC, tables 8.1–8.4 sortable with ↺, eight verdict lines per language, no overflow, 0 console errors) passes on a46240e. The h3 ids of the page carry a running counter that shifted when the chapter was added; the smoke suite now resolves headings by their printed number instead of by id. Pairs and multi remain as run on d35f7b6 (unchanged code paths).

## Addendum after the editor's review of §8 (17 Sep 2026, night; commit on top of fb4df1c)
Review items applied: §7.6's 2×2 diagonal table was emitted as broken markdown (rows through `H()`, no blank line before) and therefore never rendered — the sort tag meant for §7.6's second table landed on §7.7's table (the editor asked what sorting on 7.7 meant); both fixed. Brief index: centrality sorts descending on the first click (`data-sort-first="desc"`, honoured by the `tblsort` block). Disclosures line in the colophon (EN/RU) and README. §8 retitled "Quantum machines — 136 attempts, 14 architectures, and where they are going"; a "Terms used in this chapter" paragraph; Table 8.1's evidence columns renamed and explained; 8.3 "The idea behind each architecture"; full platform names in the map legend (`FAMN`), §7 (`FAMS`) and §8; the parallel-coordinates strip retitled with a gloss on technology = station = node; new §8.5 forecast ledger (`data/forecast-ledger.json`), the synthesis and verification subsections renumbered 8.6/8.7. Map code unchanged except the family labels (text only) and the sort-cycle rule (`data-sort-first`).

| build | suite | states | disagreements | results |
|---|---|---|---|---|
| review build (this addendum) | single | 326 | 0 | `results_c2f/` |

Smoke (`c2_smoke.py`, 152 checks: family labels, descending-first centrality, seven §8 subsections, the ledger's 8 rows, the Disclosures paragraph) passes at 1600/1280/400 px.
