# C2 brief A — machines on the Map: data embedding, Machine selector, machine card, "Used by" block (session C2, 17 Sep 2026)

Budget: 10 minutes of work; report at the end (≤ 150 words). Working directory /home/claude/qt-map (branch c2-machines-2026-09-17). You own: `build/map_js.py`, `build/page_css.py`, `build/build_html.py`. Do not touch `build/make_sections.py`, `report/`, `build/build.py`, `data/graph.json`, anything under `build/audit/`. Another agent edits the report in parallel.

## The rule to preserve (editor's, binding)
One lit set governs stations, path lines and edges: `lit = isolate ∩ focus ∩ lens`, and a machine selection is **one more term in that intersection, never a new mechanism**. Edge toggles alone decide which relation types are drawn; a selection only narrows them to the lit stations. Reset is global and must clear the machine too. Bilingual: every new UI string goes through `T('en','ru')` (see existing code) or the `lang-en`/`lang-ru` span pattern in `build_html.py`.

## Data
`data/machines.json` (136 machines): `machines[]` with id, name, org, org_id, country, family, map_path, status, status_date, physical_qubits_num, access, `layers` {"1".."10": [{node, role, gap, summary, evidence:{type,url,locator,verified}}]}, records[], codes[], evidence_counts{verified,total}; `by_node` {node_id: {primary:[ids], alternate:[ids]}}. Gap nodes start with `∅` and are NOT graph nodes. In `build_html.py`, next to `window.__GRAPH=…` (line ~351), embed a slim copy as `window.__MACH` built at build time (deterministic: same field order, `json.dumps(..., ensure_ascii=False)`): per machine keep id, name, org, family, path (=map_path), status, status_date, q (=physical_qubits_num), layers as {layerNo: [[node, role, summary, ev_type, ev_url, ev_locator, verified], …]} skipping gap nodes but counting them; plus `by_node`. Constants: register card URL `https://claude.ai/artifact/Bfj8NrxCsx8PMdxUCBMBhV#m-<id>`; technology page `https://claude.ai/artifact/2EcsTbo9kjAHseEnBxzawp#node-<node_id>`.

## UI to add
1. **Machine selector in the map bar** (`MAPUI` in build_html.py, a new `.grp` after the lens group): label "machine / машина", a `<select id="machine" class="sel">` with an empty first option ("— / —") and `<optgroup>` per family in the order SC, ION, ATOM, PHOTON, SPIN, DEFECT, TOPO, ANNEAL, options "name · org" sorted by name, populated by JS from `__MACH`. Selecting sets `state.machine = id` (null on the empty option), then `drawEdges(); dimming(); inspectMachine(...)`.
2. **The lit-set term** in `litSets()`: when `state.machine` is set, `ms` = the machine's real (non-gap) nodes on all layers, primary and alternate; `keepN = keepN ? keepN ∩ ms : ms`; `keepP = keepP ? keepP ∩ {path} : {path}` (the machine's map_path). Keep the existing "focused station is always lit" line. Stations used as *alternate* by the machine get class `altuse` (dashed outline via CSS) while lit; remove the class when no machine is selected.
3. **Machine card** (`inspectMachine(m)` in the inspector, same look as `inspect`/`inspectPath`): title = name; line: org · family · status (status_date) · q physical qubits; a "↗ register card" link (target=_blank, rel=noreferrer) to the register URL; then the 10 layers in Map order, each: layer name, station name(s) as `data-goto` links (existing pattern selects the station), role, the evidence glyph link (▣ figure ▤ paper ▥ whitepaper ▦ vendor page ▧ datasheet ▨ press ◌ inferred; title attribute = type; href = ev_url; locator shown after it in small type) and ✅/🔎; gap layers show "— (Map gap: <id>)" in muted style; footer: "evidence: ✅ n / total" and a close button that clears the machine (`state.machine=null`, select box reset). When a station is focused while a machine is selected, the station card wins (the machine stays a lit-set term; the bar summary names it).
4. **"Used by" block on the station card** (`inspect(n)`): after the existing sections, "Used by N machines (P primary · A alternate)" from `by_node`; list up to all machines (name as a `data-mach` link that selects the machine on the map; a small "↗" link to its register card; the evidence glyph + ✅/🔎 for this station's cell), primary first then alternate, grouped by family; when none: "Used by no registered machine". Keep it compact (a `<ul>` with one line per machine).
5. **Bar summary** (`window.__barSummary`): when a machine is selected, prefix "machine: <name>".
6. **Reset** (`tg-reset` handler): `state.machine=null`, the select back to the empty option, `altuse` classes cleared.
7. CSS in page_css.py: `.altuse rect.box{stroke-dasharray:4 3}`, the select's width, the machine card list styles, the used-by list; must work at 400 px (wrap, no horizontal overflow).

## Checks before you report
- `python3 build/build.py` twice → `sha256sum dist/Quantum-Technology-Map-2026.09.html` identical both times; `git diff --stat`.
- A Playwright smoke (write it as `build/audit/c2_smoke.py`, sync API, 1600×1000 and 400×800): default 96 stations lit; select machine `google-willow` → the lit stations equal its non-gap nodes and only path `sc` is lit; toggle requires on → edges only among lit stations; click station `transmon` → still lit set ⊆ machine set ∪ {transmon}, station card shows the "Used by" block with ≥ 10 machines; reset → 96 lit, machine select empty, no `altuse`; language switch to RU shows the RU labels; 0 console errors (ignore the Google Fonts ERR_TUNNEL line).
Report: what you added (one line each), the smoke results, the dist size delta, and any rule you had to interpret.
